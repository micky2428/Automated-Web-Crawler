CREATE TABLE `shopData`.`PchomeData` (
    `inspectionDate` DATE NOT NULL,                     
    `number` INT NOT NULL,  
    `productName` TEXT NOT NULL,                 
    `productId` TEXT NOT NULL,             
    `productLink` TEXT NOT NULL,          
    `productDescription` TEXT NOT NULL,          
    `bsmiStatus` VARCHAR(100) NOT NULL,     
    PRIMARY KEY(`number`)
);



